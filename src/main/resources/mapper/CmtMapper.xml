<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.boardver3.cmt.CmtMapper">
    <insert id="insCmt" useGeneratedKeys="true" keyProperty="iboardCmt">
        INSERT INTO t_board_cmt
        (iboard, iuser, ctnt)
        VALUES
        (#{iboard}, #{iuser}, #{ctnt})

    </insert>

    <select id="selCmt" resultType="CmtVo">
        SELECT iboardCmt , iboard , iuser, nm writer, writerMainPic, ctnt, created_At
        FROM t_board_cmt
        ON A.iuser = B.iuser
        ORDER BY iboard_Cmt DESC
        LIMIT ${startIdx}, ${row}
    </select>
</mapper>